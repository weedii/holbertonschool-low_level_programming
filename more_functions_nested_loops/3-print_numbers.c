#include "main.h"

/**
 * mul - checks for a digit (0 through 9)
 * Return: a*b
 * @a: the argument
 * @b: the argument
 */

void print_numbers(void)
{
    int i;

    for (i = 0; i <= 9; i++)
    {
        _putchar('0' + i);
    }
}